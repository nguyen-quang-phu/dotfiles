function brew() {
  command brew $@
  command brew bundle dump --file=~/Brewfile -f
  command chezmoi add ~/Brewfile
  echo "brew bundle dump --file=~/Brewfile"
}

function source_zsh() {
  source ~/.zshrc
  command chezmoi re-add
}
