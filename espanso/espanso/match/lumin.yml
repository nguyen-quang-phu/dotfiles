# espanso match file

# For a complete introduction, visit the official docs at: https://espanso.org/docs/

# You can use this file to define the base matches (aka snippets)
# that will be available in every application when using espanso.

# Matches are substitution rules: when you type the "trigger" string
# it gets replaced by the "replace" string.
matches:
  # Simple text replacement
  # - trigger: "@@hi"
  #   replace: "Hi there!"

  # NOTE: espanso uses YAML to define matches, so pay attention to the indentation!

  # But matches can also be dynamic:

  # Print the current date
  - trigger: "@@date"
    replace: "{{mydate}}"
    vars:
      - name: mydate
        type: date
        params:
          format: "%m/%d/%Y"

  # Print the output of a shell command
  - trigger: ":shell"
    replace: "{{output}}"
    vars:
      - name: output
        type: shell
        params:
          cmd: "echo 'Hello from your shell'"

  # And much more! For more information, visit the docs: https://espanso.org/docs/

  # Work
  - trigger: "@@review"
    replace: |
      @devwo nào mn rảnh review giúp em/mình pr này nha:
      - static:
      - templates:

  - trigger: "@@verify-staging"
    replace: "@qcwo [Card]() này có trên staging rồi, nào mn rảnh verify giúp em nha"

  - trigger: "@@verify-dev"
    replace: "@qcwo [Card]() này có trên dev rồi, nào mn rảnh verify giúp em nha"

  - trigger: "@@tag"
    replace: "luminoptimization-"

  - trigger: "@@boon"
    replace: "/boon @phongnh2 @trungdd @thinhtran @longhn @huyentt @hault2 @vytat @vyvt 1 \"  wo  \""

  - trigger: "@@deploy"
    replace: |
      Dạ em/mình xin gửi @taihnc , @trungdd, @tientm PR release nha:
      * Static:
      * Templates:
      Note: static cho team em lên testing + disable auto sync ạ.

  - trigger: "@@task_static"
    replace: |
      --- Tasks
      * [open]
      * [open] Create slide in prismic
      * [open] Update code logic display
      * [open] Add slice to all custom type
      * [open] Add slice to all templates
      * [open] Check multiple languages
      * [open] Check responsive
      * [open] Check prismic
      * [open] Push slice-machine
      * [open] Build local

  - trigger: "@@release"
    replace: |
      @devwo Em/Mình gửi trước pr release hôm nay nha:
      - lumin-templates:
      - lumin-static:
      - lumin-backend:
      - Deployment plan:
      - Release:

  - trigger: "@@titlerelease"
    replace: Release/lumin web optimization 5.x.0

  - trigger: "@@task_template"
    replace: |
      --- Tasks
      * [open]
      * [open] Check responsive
      * [open] Build

  - trigger: "@@release_checklist"
    replace: |
      [Lumin Static]: Select the necessary commits and create a release PR
      [Lumin Static]: Disable the webhook during release and enable it afterward
      [Lumin Static]: Push the new slice machine to Prismic Production.
      [Lumin Static]: Prepare documents prismic
      [Lumin Template]: Select the necessary commits and create a release PR
      [Lumin Template]: Publish templates on strapi production
      [Strapi Template]: Select the necessary commits and create a release PR
      []: Check version after swapping

  - trigger: "@@email_lumin"
    replace: phunq@luminpdf.com

  - trigger: "@@datadog_event_tracking"
    replace: "@attributes.attributes.elementName:*"

  - trigger: "@@datadog_ignore"
    replace: |
      -message:"Cannot read properties of null (reading 'addEventListener')"
      -message:"Fetch error GET https://js.hs-banner.com/cookie-banner-public/v2/cf-location"
      -message:"Fetch error GET https://js.hs-banner.com/v2/cf-location"
      -message:"Fetch error GET https://www.googleadservices.com"
      -message:"Fetch error POST https://www.google.com"
      -message:"Fetch error GET https://www.googleadservices.com"
      -message:"console error: [GSI_LOGGER]: FedCM get() rejects with NetworkError: Error retrieving a token."
      -message:"console error: [GSI_LOGGER]: Missing required parameter: client_id."
