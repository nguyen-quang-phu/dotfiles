#!/bin/sh

if [ ! -f "/home/keynold/.config/chezmoi/key.txt" ]; then
    mkdir -p "/home/keynold/.config/chezmoi"
    chezmoi age decrypt --output "~/.config/chezmoi/key.txt" --passphrase "{{ .chezmoi.sourceDir }}/key.txt.age"
    chmod 600 "/home/keynold/.config/chezmoi/key.txt"
fi
