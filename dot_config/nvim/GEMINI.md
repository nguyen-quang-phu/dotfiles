# Project Rules

- **Standardization**: All AI agents (Gemini, Cursor, Cline, etc.) MUST follow the configuration standards defined in the `.agents/` directory.
- **AstroCore**: Specifically, refer to `.agents/astrocore_standards.md`. All `astrocore` configurations MUST be edited in `lua/plugins/astrocore.lua`.

