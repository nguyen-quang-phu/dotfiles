{
  "enum": {
    "prefix": [
      "enum"
    ],
    "body": [
      "enum $1: {",
      "$2",
      "}, _suffix: true"
    ],
    "description": "Enum"
  },
  "enum1": {
    "prefix": [
      "enum"
    ],
    "body": [
      "enum $1: {",
      "$2",
      "}, _suffix: true"
    ],
    "description": "Enum"
  }
}
